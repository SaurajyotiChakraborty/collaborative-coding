// Dummy file to replace SpacetimeDB connection logic
export const getSpacetimeConnection = () => {
  return null;
};

export const disconnectSpacetime = () => { };

export const isMockMode = () => false;
